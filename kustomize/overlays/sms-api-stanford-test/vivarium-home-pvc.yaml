apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vivarium-home-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: fsx-sc-static
  resources:
    requests:
      storage: 100Gi
  volumeName: fsx-pcs-root-pv
