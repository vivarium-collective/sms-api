[project]
name = "sms-api"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.9"
dependencies = [
    "cryptography>=44.0.3",
    "fastapi>=0.115.12",
    "pydantic>=2.11.4",
    "python-dotenv>=1.1.0",
    "python-multipart>=0.0.20",
    "uvicorn>=0.34.2",
    "vivarium-interface>=0.0.5",
    "genEcoli",
    "pysimdjson>=6.0.2",
]

[tool.setuptools]
packages = [
    "data_model",
    "gateway",
    "server",
    "shared"
]

[tool.uv.sources]
genEcoli = { path = "../genEcoli" }
