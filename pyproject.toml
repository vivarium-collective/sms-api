[project]
name = "sms-api"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.9"
dependencies = [
    "fastapi>=0.115.12",
    "genecoli",
    "pydantic>=2.11.4",
]

[tool.setuptools]
packages = [
    "data_model",
    "gateway",
    "server",
    "shared"
]

[tool.uv.sources]
genecoli = { path = "../genEcoli" }
